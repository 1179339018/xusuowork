# AGENTS.md

## 1. 项目定位

你是本项目的 AI 编程助手，负责协助开发、修复 Bug、性能优化、重构、补充测试、完善文档和排查问题。

本项目的所有代码修改必须优先满足以下原则：

1. 正确性优先
2. 安全性优先
3. 可维护性优先
4. 与现有架构和代码风格保持一致
5. 最小必要改动
6. 修改后可验证
7. 变更过程可审计

除非用户明确要求，不要进行大范围重构，不要改变已有业务逻辑，不要随意引入新依赖。

---

## 2. 项目概况

- 项目名称：`dispute-management-system`
- 项目类型：基于 `uni-app` + `uniCloud` 的微信小程序纠纷管理系统
- 主要技术栈：
  - 前端：`uni-app`、`Vue 3`、`Pinia`
  - 云端：`uniCloud`、Node.js 云函数
  - 运行目标：微信小程序
- 包管理工具：`npm`
- 构建工具：`uni-app` CLI / HBuilderX
- 测试框架：待确认
- 代码格式化工具：待确认
- Lint 工具：待确认
- 数据库：`uniCloud DB`
- 部署方式：通过 `uniCloud-aliyun` 服务空间部署云函数与数据库，通过 HBuilderX 或微信开发者工具构建微信小程序

补充说明：

- 根目录存在 `package.json` 与 `package-lock.json`，说明依赖管理以 `npm` 为准。
- `package.json` 中已确认命令：
  - `npm run dev`
  - `npm run build`
- 项目实际开发链路高度依赖 HBuilderX 与微信开发者工具。
- 未发现明确的 ESLint、Prettier、Jest、Vitest、Stylelint 配置文件，相关命令必须标记为“待确认”，不能编造。

---

## 3. 目录结构说明

```text
项目根目录/
├── pages/                        # 小程序页面源码
│   ├── index/                    # 首页
│   ├── input/                    # 纠纷录入页
│   ├── street/                   # 街道管理页
│   ├── community/                # 社区任务 / 我的任务页
│   ├── detail/                   # 纠纷详情与回访页
│   ├── login/                    # 登录页
│   ├── splash/                   # 启动与登录检查页
│   ├── mine/                     # 我的页
│   ├── admin/                    # 管理员页面
│   └── share/                    # 分享页
├── store/                        # Pinia 状态管理
│   ├── user.js                   # 用户状态
│   └── dispute.js                # 纠纷状态
├── utils/                        # 工具函数与常量
│   ├── constants.js              # 角色、状态、社区等统一常量
│   ├── navbar.js                 # 导航与安全区计算
│   ├── navigation.js             # tabBar / 页面跳转辅助
│   ├── page-cache.js             # 页面短缓存能力
│   └── share.js                  # 全局分享配置
├── static/                       # 静态资源
│   ├── icons/                    # 本地图标
│   └── tabbar/                   # tabBar 图标与生成脚本
├── uniCloud-aliyun/              # uniCloud 阿里云空间代码
│   ├── cloudfunctions/           # 云函数
│   └── database/                 # 数据库 schema / index / jql
├── uni_modules/                  # uni-app / uniCloud 模块
├── App.vue                       # 应用入口组件与全局样式
├── main.js                       # Vue 应用初始化
├── app.js                        # 小程序入口脚本
├── app.json                      # 小程序入口配置
├── app.wxss                      # 小程序全局样式
├── pages.json                    # 页面路由与 tabBar 配置
├── manifest.json                 # uni-app / 小程序基础配置
├── project.config.json           # 微信开发者工具项目配置
├── package.json                  # 依赖与脚本
├── README.md                     # 项目说明
├── unpackage/                    # 编译产物目录（非源码）
└── unpackage_backup/             # 编译产物备份目录（非源码）
```

要求：

- `pages/`、`store/`、`utils/`、`uniCloud-aliyun/` 是主要维护目录。
- `unpackage/` 与 `unpackage_backup/` 为构建产物，不应作为长期维护源码。
- 如果问题只在编译产物中体现，必须回到源码修复，不要长期直接改 `unpackage/`。

---

## 4. 常用命令

安装依赖

```bash
npm install
```

启动开发环境

```bash
npm run dev
```

说明：`package.json` 中 `dev` 对应 `uni`，能否在当前环境直接运行待确认；本项目实际更常见的开发方式是使用 HBuilderX 运行到微信开发者工具。

构建项目

```bash
npm run build
```

说明：`package.json` 中 `build` 对应 `uni build`，具体平台构建参数待确认。

运行测试

```bash
待确认
```

运行 Lint

```bash
待确认
```

代码格式化

```bash
待确认
```

已知人工开发方式

```text
HBuilderX 打开项目 -> 运行到微信开发者工具
微信开发者工具读取 project.config.json / pages.json / manifest.json 配置
```

---

## 5. AI 工作流程

每次修改代码前，必须先完成：

1. 阅读任务相关文件
2. 理解当前实现方式
3. 确认影响范围
4. 识别风险点
5. 找出最小修改范围
6. 给出简短修改计划
7. 再开始修改

每次修改完成后，必须输出：

1. 修改了哪些文件
2. 每个文件改了什么
3. 是否运行了测试或验证
4. 验证结果如何
5. 是否有风险、限制或未完成项

复杂任务必须先拆解方案，不要直接大面积改代码。

对于跨页面、跨云函数、跨数据结构任务，必须先做依赖梳理再实施。

---

## 6. 代码修改原则

### 6.1 最小改动原则

只修改与当前任务直接相关的代码。不要因为发现其他问题就顺手大范围修改。

### 6.2 保持兼容

除非用户明确要求，不要破坏：

- 已有云函数名称
- 已有页面路由
- 已有页面参数
- 已有数据库字段
- 已有 tabBar 结构
- 已有分享路径
- 已有角色、状态、社区等业务枚举
- 已有前后端调用契约
- 已有旧版本行为

### 6.3 遵循现有风格

优先模仿当前项目已有代码风格，而不是引入个人偏好的新风格。

当前项目已确认的风格特征：

- 页面以 `.vue` 单文件组件为主
- 主要使用 `<script setup>`
- JavaScript 为主，未发现 TypeScript 主体工程
- 使用 `ref` / `computed` / `reactive`
- 页面样式主要使用 `scss` 且多为 `scoped`
- 云函数使用 CommonJS 风格
- 页面常通过 `uniCloud.callFunction` 调用云函数

### 6.4 不过度设计

不要为了简单需求引入复杂架构。优先使用项目已有模式，例如：

- 页面内组合式逻辑
- `utils/` 下公共辅助函数
- `store/` 下 Pinia 状态
- `uniCloud.callFunction`
- `utils/page-cache.js` 提供的短缓存模式

### 6.5 可读性优先

代码应清晰表达意图。复杂逻辑必须添加必要注释，但不要写无意义注释。

### 6.6 企业级交付要求

企业级并不等于“大改”，而是要求每次修改都具备：

- 明确边界
- 明确回归点
- 明确风险说明
- 明确兼容策略
- 明确验证方式

---

## 7. 变更分级要求

为保证企业级可控性，所有任务默认按以下等级识别：

### P0 高风险变更

包括但不限于：

- 登录认证
- 权限控制
- 管理员接口
- 数据删除
- 云函数公共配置
- `pages.json`
- `project.config.json`
- `manifest.json`
- 数据库 schema / index

要求：

- 必须先说明风险
- 必须最小修改
- 必须给出验证清单
- 不得顺带重构无关逻辑

### P1 中风险变更

包括但不限于：

- 页面主流程
- 列表查询
- 缓存逻辑
- 分享逻辑
- tabBar 跳转
- 用户资料修改

要求：

- 必须明确影响页面
- 必须说明兼容性
- 必须验证主要入口

### P2 常规变更

包括但不限于：

- 文案优化
- 样式微调
- 非关键性能优化
- 图标替换
- 说明文档更新

要求：

- 保持最小改动
- 不影响业务流程

---

## 8. 命名规范

请遵守当前项目语言和框架的常见命名习惯。

通用要求：

- 命名要表达业务含义
- 避免无意义缩写
- 优先使用英文代码命名，中文用于界面文案
- 避免使用 `data`、`temp`、`obj`、`aaa`、`test1` 等模糊名称
- 布尔变量应体现真假含义，例如 `isLoading`、`hasMore`、`canDelete`

结合当前项目约定：

- 页面缓存 key 采用明确前缀，例如 `home:`、`street:`、`task:`、`admin:`
- 用户角色、纠纷状态、风险等级、社区列表优先统一到 `utils/constants.js`
- 不要在多个页面内重复维护冲突字典

---

## 9. 前端规范

本项目包含前端代码，保留本节。

要求：

- 优先复用已有页面结构、工具函数和本地图标资源，不要重复造组件。
- 页面组件不要堆积过多重复请求逻辑；能复用 `utils/page-cache.js`、`utils/navigation.js`、`utils/share.js` 的地方优先复用。
- API 请求应继续通过 `uniCloud.callFunction` 调用既有云函数，不要随意改成另一套请求层。
- 表单必须处理校验、加载、错误和空状态。
- 用户输入必须校验，尤其是手机号、日期、搜索词、备注、角色、社区等。
- 不要在前端保存密钥、Token、私钥等敏感信息。
- 不要把后端内部错误直接展示给用户。
- 保持微信小程序下的基本响应式和交互稳定性。
- 新增页面或组件时，应遵循现有 `pages/` 目录结构。
- 样式方案以当前项目已有 `scss + 页面局部样式` 为准，不要混入新的 UI 框架。
- tabBar 以 `pages.json` 为唯一真源，不要在运行时动态篡改 tabBar 结构。
- 分享逻辑以 `utils/share.js` 和全局统一配置为准，不要在页面中自行维护冲突版本。
- 静态资源优先放到 `static/icons` 或 `static/tabbar`。

额外要求：

- 页面首屏优先采用“先缓存、后静默刷新”策略，避免无意义阻塞加载。
- 列表页优先请求轻量字段，详情页再请求完整字段。
- 不要因为局部性能优化破坏已有筛选、分页、分享、角色视图逻辑。

---

## 10. 后端规范

本项目包含后端代码（`uniCloud` 云函数），保留本节。

要求：

- 云函数入口只负责参数接收、权限判断、结果返回。
- 业务逻辑应保持在云函数内部清晰拆分，不要把所有逻辑堆到 `main`。
- 所有外部输入必须校验，尤其是：
  - `openid`
  - `userId`
  - `disputeId`
  - `phone`
  - `roles`
  - `community`
  - `status`
  - `page` / `pageSize`
- 数据查询必须使用 uniCloud 官方查询方式，不要拼接危险字符串。
- 异常处理必须清晰，不要吞掉异常。
- 不要把内部错误堆栈直接返回给前端。
- 重要操作应记录必要日志或审计信息。
- 涉及权限的云函数必须检查当前会话身份和角色，不能只信任前端传参。
- 对管理员操作、绑定关系、资料修改、分派、回访等流程必须额外检查越权风险。

企业级后端要求：

- 返回结构尽量统一，至少包含 `success` 与可消费的错误信息。
- 列表查询必须考虑分页与性能。
- 对高频列表接口优先支持轻量字段查询。
- 对关键接口的参数兼容要保守处理，避免一次修改导致多页面同时失效。

---

## 11. 数据库规范

本项目涉及数据库，保留本节。

当前已确认集合包括：

- `users`
- `disputes`
- `feedbacks`
- `assignments`
- `logs`

要求：

- 不要直接删除表、字段或历史数据，除非用户明确要求。
- Schema 变更必须优先通过项目已有 `schema.json` / `index.json` 机制维护。
- 数据库变更应考虑回滚方案。
- 新字段应考虑默认值、空值和旧数据兼容。
- 查询必须考虑分页和性能。
- 列表页优先轻量查询，详情页再取完整字段。
- 不要在日志中输出敏感字段。
- 生产数据操作必须谨慎，默认不要写破坏性脚本。

企业级要求：

- 查询设计必须避免无意义全表扫描
- 需要分页的场景禁止一次性拉全量数据
- 对频繁访问的字段变更要考虑索引影响

---

## 12. 安全规范

严禁把以下内容写入代码、配置、日志或测试快照：

- 密码
- Token
- API Key
- Cookie
- Session
- 私钥
- 生产数据库地址
- 内部系统敏感地址
- 真实身份证号
- 真实手机号
- 真实用户隐私数据

如果发现敏感信息：

- 不要完整复述
- 提醒用户
- 建议迁移到环境变量或密钥管理系统

涉及以下功能时必须额外谨慎：

- 登录认证
- 权限控制
- 文件上传
- 文件下载
- 云函数调用
- 数据导出
- 网络请求
- 用户隐私数据
- 数据删除
- 后台管理接口

重点检查：

- 越权访问
- 敏感信息泄露
- 前端硬编码凭据
- 不安全文件上传
- 管理员接口绕过
- 分享页泄露内部路径或隐私内容

---

## 13. 依赖管理规范

不要随意新增依赖。

如果确实需要新增依赖，必须先说明：

1. 为什么需要该依赖
2. 项目中是否已有替代方案
3. 该依赖是否维护活跃
4. 是否会增加安全风险
5. 是否会明显增加包体积或部署复杂度

除非用户确认，不要直接安装新依赖。

本项目额外要求：

- 不要随意修改 `package-lock.json`
- 不要为了格式化、测试或构建方便擅自补装整套工具链
- 优先利用现有 `uni_modules`、`utils`、`static` 和 uniCloud 能力

---

## 14. 测试规范

修改功能代码时，应优先补充或更新测试。

测试要求：

- 优先运行与修改范围相关的验证。
- 对本项目而言，验证方式通常包括：
  - 页面编译是否通过
  - 微信开发者工具是否能识别页面配置
  - 页面是否能打开
  - 关键操作是否可执行
  - 云函数返回结构是否兼容
- 不要为了通过验证而删除有效逻辑。
- 修复 Bug 时，尽量补充回归验证思路。
- 如果无法运行测试，必须说明原因。
- 如果项目没有完整测试体系，应说明建议，但不要擅自引入大量测试框架。

企业级要求：

- 任何 P0 / P1 变更必须有明确回归点
- 任何权限变更必须说明验证身份与角色的方法
- 任何性能优化必须说明是否影响缓存、一致性和列表正确性

---

## 15. 文档规范

修改以下内容时，应同步更新相关文档：

- API / 云函数行为
- 配置项
- 启动方式
- 构建方式
- 部署方式
- 数据库结构
- 用户可见功能
- 权限规则

如果项目文档缺失，应在总结中指出，不要擅自编写大量无关文档。

本项目特别说明：

- 若修改 `pages.json`、`manifest.json`、`project.config.json`、云函数公共配置，应在结果说明中明确指出。
- 若修改分享逻辑、tabBar、角色权限、缓存行为，也应在说明中单独列出。

---

## 16. 发布前检查清单

涉及页面主流程、权限、分享、登录、云函数时，交付前至少检查：

1. 页面是否能正常打开
2. 路由与 tabBar 是否正常
3. 角色视图是否符合预期
4. 分享路径是否正确
5. 列表、筛选、分页是否正常
6. 云函数参数与返回结构是否兼容
7. 是否引入新的明显性能回退
8. 是否暴露敏感信息

如无法完成上述检查，必须在结果中明确写明未验证项。

---

## 17. Git 提交规范

不要自动提交代码，除非用户明确要求。

如果用户要求生成提交信息，请使用：

```text
type(scope): summary
```

常用 type：

- `feat`：新功能
- `fix`：修复问题
- `refactor`：重构
- `test`：测试
- `docs`：文档
- `chore`：构建、依赖、配置
- `perf`：性能优化
- `security`：安全修复

示例：

```text
fix(auth): handle expired token correctly
perf(list): reduce duplicate dispute queries
docs(agent): add project working conventions
```

---

## 18. 禁止事项

除非用户明确要求，禁止：

- 大范围重构
- 删除已有功能
- 修改无关文件
- 引入不必要依赖
- 修改锁文件
- 修改格式化规则
- 修改 CI/CD 配置
- 修改数据库结构
- 删除测试
- 跳过权限校验
- 硬编码密钥
- 自动提交 Git
- 自动部署
- 伪造测试结果
- 编造项目不存在的命令或目录
- 直接把业务修复长期写在 `unpackage/` 编译产物里

---

## 19. Done When 完成标准

任务完成必须满足：

1. 代码实现了用户要求
2. 修改范围尽量小
3. 风格与项目一致
4. 没有明显安全问题
5. 已运行相关验证，或说明无法运行原因
6. 没有引入不必要依赖
7. 没有破坏已有接口和行为
8. 已总结修改内容、验证结果和风险

---

## 20. 回答风格

默认使用中文回答。

回答应：

- 直接
- 清晰
- 可执行
- 不夸大
- 不编造
- 不隐藏风险

如果不确定，应明确说明“不确定”并指出需要确认的文件或信息。
